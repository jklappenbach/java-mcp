package io.javamcp.server;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/** Scaffold smoke test (unit 0): proves server compiles and JUnit 5 runs. */
class SmokeTest {
    @Test
    void junitRuns() {
        assertTrue(true);
    }
}
