package io.javamcp.skill;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/** Scaffold smoke test (unit 0): proves skill-core compiles and JUnit 5 runs. */
class SmokeTest {
    @Test
    void junitRuns() {
        assertEquals(2, 1 + 1);
    }
}
